const initialState = {
  shape: 'triangle'
}

export default function userstate(state = initialState, action) {

  switch (action.type) {
    //case ACTION_TYPE

    default:
      return state
    }
}
